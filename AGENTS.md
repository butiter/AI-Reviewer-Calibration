# Agent Review 工作流

本文件由主 Agent 读取并执行，仅在用户要求审阅论文时运行。评测 Subagent 只接收固定评审 prompt 和匿名论文输入。

概述：先在本地把 PDF 转成**按阅读顺序排列的文字和插图**，再与固定评审 prompt 一起提供给 Subagent。

- 主 Agent 读取文件，使用 PyMuPDF 逐页读取文本及其页面坐标，覆盖正文、参考文献和附录，并保留页码。可提取的表格尽量以文字形式保存。
- 提取插图，结合 PDFFigures2、版面检测和校正结果定位图像区域，导出原始位图或局部裁剪图。图中的标注保留在图片里。检查遗漏、重复和误把正文截成图片的情况，不用整页截图替代正文。
- 处理本稿作者姓名、单位、邮箱等身份信息，保留其他文献作者，并检查已知作者姓名是否残留。
- 文字保存为 `paper.txt` 和 `text.json`，插图单独保存；`index.json` 记录文字、图片路径及交错顺序。
- 主 Agent 或调用程序按索引读取文字和图片，在最前面加入固定评审指令，构成一条多模态输入，再调用 Subagent：

  ```text
  固定评审指令 → 第1页文字 → 插图 → 后续文字 → 第2页文字 → … → 参考文献和附录
  ```

图片路径由调用程序解析，实际图片内容随请求提供给模型。评测 Subagent 开始推理时，文字和插图已经加载完毕，因此无需自己读取 PDF、访问文件或调用工具。不能只把文件路径写进 prompt，让 Subagent 自己打开。

可以查看 [protocol.md](protocol.md) 来获取实际使用的 prompt；也可以查询 `2025/`、`2026/` 中任意论文 JSON 的 `ai_reviews`，查看历史审阅方式。这些 JSON 是评审结果，不是论文输入包。

## 五次独立评审

- 对一篇论文调用五个独立 Subagent，各审阅一次，使用 **GPT-5.6 Sol，medium effort**；配置值为 `gpt-5.6-sol` 和 `medium`。
- 这里的 Subagent 指独立的评测会话。历史实验通过 Codex app-server 创建会话并传入文字和图片。执行时必须使用支持预加载图片的调用接口；如果当前接口只能传文字，应说明无法按本协议运行，不能把图片路径当作图片内容。
- 五次使用同一份匿名论文输入、同一份 prompt 和同一返回格式。每次都创建新的空会话，不继承主 Agent 的聊天历史或其他评审结果。并发数受运行环境限制时，可以分批执行。
- 在配置支持的范围内禁用工具、联网和外部上下文，并明确要求 Subagent 不搜索、不读文件、不加载技能、不继续调用其他 Agent。评审结束后核对工具调用记录；实际调用了工具的结果不能算作有效评审。
- 主 Agent 保管作者映射、人类评分、录用结果和其他 AI 评审；这些内容不传给评测 Subagent。评测 Subagent 之间不共享上下文或结果文件。
- 保留五份原始返回。不能因为分数不理想而重跑、挑选或替换结果；技术失败需要补跑时，保留失败记录并说明原因。

## 汇报分数和估算中稿率

- 汇报五次评分序列、平均分、样本标准差，以及评审中反复出现的主要优点和问题。评分为 1–10 的整数，confidence 为 1–5，返回字段遵循 `protocol.md`。
- 平均分直接按五次评分计算。例如 `66655` 的实际均分是 5.6；如果另外用“5.5 档”描述相邻档位分歧，应明确它是展示档位，不能替代实际均分。
- 由主 Agent 在五次评审完成后读取历史 JSON，估算中稿率。用户没有指定参照年份时，分别报告 2025 和 2026，不合并两个年份。
- 估算时说明分组规则、统计单位和分母。README 的接收占比按单次评分统计；如果按五次均分或“5.5 档”汇报，应先用历史 JSON 按相同口径重新统计，每篇论文只计一次。不能把五次均分直接代入单次评分的接收率表。`status` 以 `accepted` 开头视为接收；默认分母包含撤稿和桌拒，如改变口径需说明。
- 同时报告已接收数量、总数量和百分比；没有对应样本就说明无法估计，样本很少就注明参考有限。这是当前模型、prompt 和分层样本下的经验参照，不是新论文的确定中稿概率。

最终汇报包含：五次原始评分、平均分、样本标准差、按年份区分的参考接收占比及样本量、主要评审意见，以及任何输入缺失或运行异常。
